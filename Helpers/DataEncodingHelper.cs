
namespace Transport.EGTS.Helpers
{
	/// <summary>
	/// хелпер для кодирования\декодирования данных
	/// </summary>
	internal class DataEncodingHelper
	{
		internal byte[] DecodedBytes(byte[] encodedBytes)//,EncodingEnum encoding)// кодировки пока нет!
		{
			return encodedBytes;
		}

		internal byte[] EncodeBytes(byte[] rawBytes)//,EncodingEnum encoding)// кодировки пока нет!
		{
			return rawBytes;
		}
	}
}
